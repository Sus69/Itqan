# Changelog Rule

- **Strict Append-Only Policy**: Never delete, edit, or modify existing historical entries in `docs/CHANGELOG.md` (or any other changelog file).
- **Only Add**: When logging new milestones, changes, or releases, only append new entries to the changelog.
